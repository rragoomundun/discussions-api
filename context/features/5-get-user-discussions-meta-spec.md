# Get user discussions meta

## Overview

Get meta informations on all user's discussions.

## Requirements

- Create /user/:id/discussions/meta
- Returns field nbPages (the total number of pages for all discussions results)
