# Get Forum Meta

## Overview

Get extra informations when fetching the forums.

## Requirements

- When calling the route /forum returns for each forum: nbDiscussions (the number of discussions), nbMessages (the number of messages), lastMessage: { discussion: {id, name} , date}
