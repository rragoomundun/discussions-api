# Search pages

## Overview

Get the number of pages for a search query.

## Requirements

- Create route GET /search/meta
- Return { nbPages } which correspond to the number of pages for a specific search query
