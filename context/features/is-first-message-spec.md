# Is First Message

## Overview

Returns whether a message is the first one in a dicussion.

## Requirements

- Create route {GET} /message/:messageId/is-first
- Return the following object {isFirst} where isFirst equals true if it is the first message in a discussion otherwise false
