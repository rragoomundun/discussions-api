# Get Discussion in Forum Meta 2

## Overview

Modify the `/discussion/all` endpoint to add more meta information.

## Requirements

- Return the category of the discussions as follow `category {id, name}`
- Return the forum of the discussions as follow `forum {id, name}`
- Put all the discussions in an array `discussions`
