# Create discussion

## Overview

Create a new discussion

## Requirements

- Create discussion model. It contains these fields: id (integer, primary key), title (string), open (boolean), createdAt (date), forumId
- Create the appropriate migration
- Create route [POST] /discussion to create a new discussion
