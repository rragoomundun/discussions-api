# Delete Message

## Overview

Delete a user message.

## Requirements

- Create route [DELETE] /message/:messageId
- Delete the message that correspond to message id

## Notes

- Only a moderator or the admin can delete a message
- A moderator cannot delete the admin message
