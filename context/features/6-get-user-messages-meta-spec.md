# Get user messages meta

## Overview

Get meta informations on all user's messages.

## Requirements

- Create /user/:id/messages/meta
- Returns field nbPages (the total number of pages for all messages results)
